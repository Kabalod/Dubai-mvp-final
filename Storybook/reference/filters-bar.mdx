import { Meta, Preview } from '@storybook/blocks';
import FiltersBar from '../components/analytics/filters-bar';

<Meta title="Analytics/FiltersBar" />

# FiltersBar

Панель фильтров.

<Preview>
  <FiltersBar filters={[{ key: 'status', label: 'Active' }]} onChange={() => {}} />
</Preview>
