import { Meta, Preview } from '@storybook/blocks';
import SegmentedGroup from '../components/segmented/segmented-group';

<Meta title="Design System/SegmentedGroup" />

# SegmentedGroup

Группа сегментов (переключатель).

<Preview>
  <SegmentedGroup options={[{ label: 'A', value: 'a' }, { label: 'B', value: 'b' }]} value={'a'} onChange={() => {}} />
</Preview>
